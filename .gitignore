node_modules/
web/dist/

# Runtime verification output. Curated copies live in docs/media/ (tracked).
/media/
playwright-report/
test-results/
release-report.json

.env
.env.local
*.log
.DS_Store

# Per-developer agent config. The shared instructions and skills are tracked.
.claude/settings.local.json

# Local PAT used to exercise the attachment upload by hand. In CI this comes
# from the MEDIA_GH_TOKEN secret and is never written to disk.
token
