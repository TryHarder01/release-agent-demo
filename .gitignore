node_modules/
web/dist/

# Runtime verification output. Curated copies live in docs/media/ (tracked).
/media/
playwright-report/
test-results/
release-report.json

# Guard only — the gcloud SDK belongs in ~/google-cloud-sdk, not in the repo.
# It is ~445M and nothing here shells out to a vendored copy.
google-cloud-sdk/
google-cloud-cli-*.tar.gz

.env
.env.local
*.log
.DS_Store

# Per-developer agent config. The shared instructions and skills are tracked.
.claude/settings.local.json
warpdemo-505821-9771c26edc3f.json
# Grafana's copy/pointer to a GCP service-account key. The key itself is
# ignored by name above; these guard the local wiring too.
grafana/gcp-key.json
grafana/.env
